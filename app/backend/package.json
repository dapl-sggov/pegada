{
  "name": "fpl-backend",
  "version": "2.0.0-simplificado",
  "description": "FPL Ponte — versão simplificada (Node + SQLite + Entra ID + ficheiros). Sistema temporário até SmartLegis/IntGov.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "seed": "node src/seed.js",
    "backup": "node -e \"import('./src/backup.js').then(m=>m.correrBackup()).then(r=>{console.log(JSON.stringify(r,null,2));process.exit(0);})\"",
    "test": "node --test --test-force-exit --test-timeout=30000 test/*.test.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.21.0"
  },
  "comment": "Apenas duas dependências: cookie-parser e express. Tudo o resto (SQLite, hash, crypto) vem do standard library do Node 22+."
}
