{
  "name": "fpl-backend",
  "version": "1.0.0-rc",
  "description": "FPL Ponte — Backend (Node.js + Express + PostgreSQL/SQLite)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "seed": "node src/seed.js",
    "test": "node --test --test-force-exit --test-timeout=60000 test/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2"
  },
  "optionalDependencies": {
    "pg": "^8.13.0",
    "ioredis": "^5.4.1",
    "@aws-sdk/client-s3": "^3.700.0",
    "ldapts": "^7.3.0",
    "nodemailer": "^6.9.0"
  },
  "comment": "Todas as optionalDependencies são activáveis por configuração: em desenvolvimento o sistema corre sem nenhuma delas (SQLite + cache in-memory + filesystem + driver `local` + outbox). Em produção (RING): pg/ioredis/@aws-sdk para infra; ldapts para o diretório interno; nodemailer para o SMTP do Estado."
}
